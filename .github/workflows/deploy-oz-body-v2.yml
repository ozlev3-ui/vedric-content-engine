name: Deploy MYBA Flow PWA

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-oz-body-v2.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout MYBA Flow
        uses: actions/checkout@v4
        with:
          ref: oz-body-app

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload MYBA Flow artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: oz-body

      - name: Deploy MYBA Flow
        id: deployment
        uses: actions/deploy-pages@v4

# MYBA Flow MVP deployment - 2026-07-27